The previous response was invalid.

Original user request:
{user_prompt}

Invalid response:
{invalid_response}

Error:
{error}

Return a corrected response as valid JSON.

Return JSON only.

Do not include markdown or code fences.